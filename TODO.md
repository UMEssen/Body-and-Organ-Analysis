# TODO's

- Fix scikit deprecated warnings
- New README.md file
- CNR CI/CD pipeline tests
- Windows and MacOS example scripts
- Check what happens when sending files with illegal folder name characters (Test)
- Add MacOS fix for DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib:$DYLD_FALLBACK_LIBRARY_PATH python tests/test_boa.py
and check the installation of the homebrew package needed
